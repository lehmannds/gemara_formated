# Popup (modal) styles

Block **`popup`**: fixed overlay, backdrop control, dialog panel, header with title and close control, scrollable body. Pairs with `createPopup()` in [`js/popup.js`](../../js/popup.js). Source: [`css/popup.css`](../../css/popup.css).
